# 版本命名指南（yzzx-content-studio · 言直在线选题组）

格式是 **`x.y.z`**（如 `2.2.0`）——三段数字各有各的用法。
**不是所有更新都动 `x`**：恰恰相反，绝大多数更新只动 `y` 或 `z`。
`x` 只在**旧调用方式会失效**时才 +1，一年可能只动一次，也可能一次都不动。

> 版本号由 `release.py` 自动递增，**禁止手改** `plugin.json` 的 `version`。

---

## 一、一句话判据

> 老用户按老提法问问题，还能不能拿到对的结果？

| 结论 | 升位 | 性质 |
|---|---|---|
| **不能**——角色变了、工作流结构变了 | **x**（major） | 破坏性，`y.z` 归零 |
| **能**，但包里多了新本事 / 多了一道必经步骤 | **y**（minor） | 向后兼容的能力增加，`z` 归零 |
| **能**，只是改字、改词、改数字、换图 | **z**（patch） | 修字面，能力没变 |

---

## 二、三段各管什么

### `x`（MAJOR）—— 破坏性 / 结构性
命中任一即 +1，且 `y.z` 归零：

- 新增或删除角色（成员 Agent）
- 角色改名、Agent ID 改名、`skills/` 目录改名
- **工作流编号结构变化**（W1–W6 增删、重排、职责重划）
- `plugin.json` 的 `name` / `agentName` 变动
- 任何让「老话术调不通、老流程走不下去」的改动

> 例：`1.0.0 → 2.0.0`。删掉曲同工（风格翻译官）、删掉封面工作流、工作流整体重排 —— 老用户按旧流程提问会错，属于破坏性改动。

### `y`（MINOR）—— 向后兼容的能力增加
老用法照常能用，但包「多会了一点」：

- 新增 IP 兜底卡、新增 `references/` 参考文件
- 新增 skill
- 新增 quickPrompt
- 新增一条工作流（W7…）
- **给现有角色加新职责、加新的必经步骤**（如新增「连接器前置自检」）
- 补充 ima 库 ID

> 例：`2.0.0 → 2.1.0`（取消 7 张卡上的「直接出稿」例外，全包字数统一 2 分钟——流程规则变了，但调用方式没变）。
> 例：`2.1.0 → 2.2.0`（新增前置自检、版本号上卡）。

### `z`（PATCH）—— 修字面，不动能力
- 错别字、标点、措辞微调
- 别名表补充
- 字数 / 时长参数微调（**仅当这一版只动参数、不加新能力也不加必经步骤时**；若同一版还夹带了新交互 / 新字段 / 新流程步骤，按更高的那一档走）
- 头像替换、README 修订

> 例：`2.2.0 → 2.2.1`（把「2 分鐘」的错字改掉）。
> 例：`2.3.0 → 2.4.0` —— 表面看只是「字数从 720–880 收到 ≤700」，但同时新增了选题池的「本轮不做 / 自定义想法」交互、Notion 两个新字段、并砍掉发布包 HTML，属于**多了新本事 + 多了一道必经步骤**，走 `y` 而非 `z`。

---

## 三、决策树（照顺序问）

1. **角色 / Agent ID / skill 目录 / 工作流编号变了吗？**
   → 是 → **x + 1**
2. **没变。包里多了新能力、或多了一道必经步骤吗？**
   → 是 → **y + 1**
3. **都没有，只是改字、改数字、换图？**
   → 是 → **z + 1**
4. **都算不上？** → 那多半是「知识/风格」，**根本不该发包**（见第四节）。

---

## 四、不升版本、不发包的改动（走云端，改完即时生效）

以下内容**高频变动**，设计上就放在 Notion / ima，改完全员下次调用即时生效：

- IP 人设卡的定位、受众、语气、口头禅、字数、禁忌
- 新增一个 IP（在 Notion 建 `IP卡-<名字>` 即可）
- 合规词库、限流词表
- ima 库的新增内容

> **判据**：改的是「**知识 / 风格**」→ 走 Notion，**不发包**；
> 改的是「**流程 / 角色**」→ 才发包，按第一至三节升位。

---

## 五、常见误用

| 误判 | 为什么错 | 正确做法 |
|---|---|---|
| 「改了 7 张人设卡，动静挺大，升 major 吧」 | 人设卡是**知识**不是结构，而且压根不该进包（走 Notion） | 正常：**不发包**；若因故必须进包 → `z` |
| 「加了一条铁律 → 升 major」 | 没删角色、没改编号，老提法依然有效 | `y` |
| 「优化了一句措辞 → 升 minor」 | 没有新增能力，只是改字 | `z` |
| 「每次发包顺手 +1 major」 | `x` 会迅速膨胀，失去「破坏性」的信号意义 | 按判据来，多数是 `y` 或 `z` |
| 「改了 `displayDescription` 文案 → 单独发一版」 | 卡片文案是元数据，通常搭在别的改动一起发 | 搭车发布，不必单开一版 |

---

## 六、版本号必须保持一致的地方

同一版里，以下位置的版本号应当一致（前两处由脚本自动同步）：

| 位置 | 谁写 |
|---|---|
| `.codebuddy-plugin/plugin.json` 的 `version` | `release.py`（**唯一真源**） |
| `plugin.json` 的 `displayDescription` 尾部 `vX.Y.Z` | `release.py` 自动同步 —— **专家卡片上直接可见** |
| `README.md` 顶部版本行 | `release.py` 自动同步 |
| `agents/yzzx-content-studio-team-lead.md` 顶部版本行 | `release.py` 自动同步 |
| `CHANGELOG.md` 新条目 | `release.py` |
| 工作空间两份文档（说明书 / 项目配置包） | 用 `--sync-doc` 传入即自动同步 |

---

## 七、发布流程

```bash
PY=/Users/tychowu/.workbuddy/binaries/python/versions/3.13.12/bin/python3

# 默认 patch（最常用）
$PY ~/.workbuddy/skills/expert-release/scripts/release.py -m "补充舒婷别名"

# 明确升位
$PY ~/.workbuddy/skills/expert-release/scripts/release.py --bump minor -m "新增连接器前置自检"
$PY ~/.workbuddy/skills/expert-release/scripts/release.py --bump major -m "新增分发官角色"

# 连带同步工作空间文档的版本行
$PY ~/.workbuddy/skills/expert-release/scripts/release.py --bump minor -m "xxx" \
  --sync-doc ~/WorkBuddy/2026-09-21-10-40-47/言直在线选题组-说明书.md \
  --sync-doc ~/WorkBuddy/2026-09-21-10-40-47/项目配置-连接器与指令.md
```

脚本自动完成：版本递增 → 同步各处版本号 → 写 `CHANGELOG.md` → `validate` → `register` → 打包 `yzzx-content-studio-v<版本>.zip`。
**校验失败会中止并回滚**（含已同步的文档），修完再跑。

---

## 八、发完还要做的两步（脚本不会代劳）

1. **企业后台上传**：企业管理后台 → 专家管理 → 新增版本 → 上传 zip → **保存并启用**（只点「仅保存」是草稿，成员看不到）
2. **项目资产库留档**：把 zip 传一份过去，方便同事回退到旧版本

## 九、本地即时生效（本机）

企业后台是给同事用的。**自己这台电脑**要立刻用上新版，把源目录覆盖进安装缓存即可：

```bash
CACHE=~/.workbuddy/plugins/cache/my-experts/yzzx-content-studio/<新版本>
cp -R ~/.workbuddy/plugins/marketplaces/my-experts/plugins/yzzx-content-studio "$CACHE"
# 再把 installed_plugins.json 里该插件的 installPath / version 指到新版本
```

## 十、回退

项目资产库里留着历史 zip。回退 = 后台重新上传旧版本 zip 并启用；
本地则把缓存里的 `installPath` 指回旧版本目录（`cache/` 下旧的版本目录会保留）。
